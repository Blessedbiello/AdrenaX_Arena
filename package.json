{
  "name": "adrenax-arena",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter arena-server dev & pnpm --filter arena-ui dev",
    "dev:server": "pnpm --filter arena-server dev",
    "dev:ui": "pnpm --filter arena-ui dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter arena-server test",
    "db:migrate": "pnpm --filter arena-server db:migrate",
    "db:generate": "pnpm --filter arena-server db:generate",
    "lint": "pnpm -r lint"
  },
  "engines": { "node": ">=20" }
}
